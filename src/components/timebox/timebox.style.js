import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    timeBox: {
        backgroundColor: COLORS.blue,
        padding: 20,
        marginVertical: 7,
        borderRadius: 6,
        alignItems: 'center'
      },
      timeText: {
        color: COLORS.white,
        fontSize: FONT_SIZE.md
    },
};