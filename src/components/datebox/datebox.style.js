import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    dateBox: {
        backgroundColor: COLORS.blue,
        padding: 20,
        marginVertical: 7,
        borderRadius: 6,
        alignItems: 'center'
      },
      dateText: {
        color: COLORS.white,
        fontSize: FONT_SIZE.md
    }
}