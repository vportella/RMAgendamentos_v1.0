import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    header: {
        width: "100%",
        alignItems: "center"
    },
    logo: {
        width: 255,
        height: 95,
        marginBottom: 50,
        marginVertical: 30

    },
    titulo: {
        color: COLORS.dark_gray,
        fontSize: FONT_SIZE.md,
        textAlign: "center",
    }
}