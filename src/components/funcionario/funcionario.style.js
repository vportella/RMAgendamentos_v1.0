import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    button: {
        backgroundColor: COLORS.blue,
        padding: 22,
        marginVertical: 10,
        borderRadius: 6,
        width: 310,
        alignItems: 'center',
    },
    buttonText: {
        color: COLORS.white,
        fontSize: FONT_SIZE.md,
    },
};